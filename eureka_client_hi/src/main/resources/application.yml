server:
  port: 8763
spring:
  application:
    name: service-hi
  redis:
    host: localhost
    port: 6379
    password:
    timeout: 6000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    database: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/